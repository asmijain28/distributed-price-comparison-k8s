apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraper-flipkart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: scraper-flipkart
  template:
    metadata:
      labels:
        app: scraper-flipkart
    spec:
      containers:
        - name: scraper-flipkart
          image: ghcr.io/OWNER/scraper-flipkart:latest
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: price-comparison-config
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: scraper-flipkart-svc
spec:
  type: ClusterIP
  selector:
    app: scraper-flipkart
  ports:
    - port: 8002
      targetPort: 8002

