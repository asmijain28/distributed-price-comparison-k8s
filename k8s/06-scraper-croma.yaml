apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraper-croma
  labels:
    app: priceradar
    component: scraper-croma
spec:
  replicas: 2
  selector:
    matchLabels:
      component: scraper-croma
  template:
    metadata:
      labels:
        app: priceradar
        component: scraper-croma
    spec:
      containers:
        - name: scraper-croma
          image: ghcr.io/OWNER/priceradar-scraper-croma:latest
          ports:
            - containerPort: 8006
          envFrom:
            - configMapRef:
                name: priceradar-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: scraper-croma-svc
  labels:
    app: priceradar
    component: scraper-croma
spec:
  selector:
    component: scraper-croma
  ports:
    - port: 8006
      targetPort: 8006
  type: ClusterIP
